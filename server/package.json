{
  "name": "toolshed-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Capstone \u2014 Kirkgate Toolshed: tools and loans, auth with roles.",
  "scripts": {
    "dev": "node --watch src/index.js",
    "start": "node src/index.js",
    "seed": "node src/seed.js",
    "test": "node --test --experimental-test-coverage \"test/**/*.test.js\"",
    "test:watch": "node --test --watch \"test/**/*.test.js\""
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "better-sqlite3": "^11.0.0",
    "cookie-parser": "^1.4.6",
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "compression": "^1.7.4"
  },
  "devDependencies": {
    "supertest": "^7.0.0"
  }
}
